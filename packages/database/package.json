{
  "name": "@repo/database",
  "version": "0.0.1",
  "private": true,
  "main": "./client/index.js",
  "types": "./client/index.d.ts",
  "exports": {
    ".": {
      "types": "./client/index.d.ts",
      "default": "./client/index.js"
    }
  },
  "scripts": {
    "db:generate": "dotenv -e ../../.env -- prisma generate",
    "db:migrate": "dotenv -e ../../.env -- prisma migrate dev --name auto_migration",
    "db:migrate:deploy": "dotenv -e ../../.env -- prisma migrate deploy",
    "db:migrate:status": "dotenv -e ../../.env -- prisma migrate status",
    "db:migrate:resolve": "dotenv -e ../../.env -- prisma migrate resolve",
    "db:push": "dotenv -e ../../.env -- prisma db push",
    "db:reset": "dotenv -e ../../.env -- prisma migrate reset --force",
    "db:studio": "dotenv -e ../../.env -- prisma studio",
    "db:seed": "dotenv -e ../../.env -- tsx prisma/seed.ts",
    "build": "prisma generate",
    "clean": "rimraf client node_modules .turbo"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0"
  },
  "devDependencies": {
    "prisma": "^6.18.0",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "bcrypt": "^6.0.0",
    "@types/bcrypt": "^6.0.0",
    "dotenv-cli": "^7.4.2"
  }
}
